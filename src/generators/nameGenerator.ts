import Landform from '../enums/landform';
import Names from '../lists/names';
import Nouns from '../lists/nouns';

// TODO do I generate a zone and determine special characteristics, then find a name to match?
// Look at old D&D DM Manual of the Planes for info on creating zones

export default class NameGenerator {
  public zoneName(zoneType: Landform): string {
    const name = '';

    return name;
  }

  public mobName(): string {
    return '';
  }
}
